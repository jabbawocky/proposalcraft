# Product Hunt Gallery Images

**3 of 4 PNGs are committed and ready to upload** — `image1-brief-analysis.png`, `image3-install.png`, `image4-freemium.png`. Only image 2 (animated GIF) still needs a screen recording from Mat.

| File | What it shows | Status |
|---|---|---|
| `image1-brief-analysis.png` ✅ | Client email → analyze_brief output (before/after) | **Ready — upload directly** |
| `image2-core-loop.html` | **Animated** — full workflow: type prompt → tool call → proposal streams in | Needs screen recording (see below) |
| `image3-install.png` ✅ | 3-line JSON install block | **Ready — upload directly** |
| `image4-freemium.png` ✅ | Freemium counter + upgrade CTA in Claude Desktop | **Ready — upload directly** |

## Image 2 — animated HTML (no Mac recording needed)

`image2-core-loop.html` is a self-contained animation that runs entirely in the browser:

1. **Open** `image2-core-loop.html` in Chrome (or use the GitHub Pages URL once pushed)
2. **Start a screen recorder** — Mac's built-in Screenshot app (⌘+Shift+5), Loom, Kap, whatever you have
3. **Wait ~15 seconds** for one full loop to complete
4. **Stop recording** → save as GIF (drag MP4 to ezgif.com/video-to-gif if needed, optimize to <5MB)

Caption for PH: **"30 seconds. Ready to send."**

The animation loops automatically, so if the first take is off, just wait for the next loop.

## Upload order for PH

1. image1 (brief → analysis) — most visually striking, goes first
2. image2 (GIF — browser recording of the animation)
3. image3 (install simplicity)
4. image4 (freemium gate)
